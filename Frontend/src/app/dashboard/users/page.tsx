'use client';

// Wrapper to render existing top-level users page inside dashboard layout
import UsersPage from '@/app/users/page';

export default function DashboardUsersPage() {
  return <UsersPage />;
}
