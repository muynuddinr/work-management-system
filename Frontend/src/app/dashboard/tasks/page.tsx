'use client';

// Wrapper to render existing top-level tasks page inside dashboard layout
import TasksPage from '@/app/tasks/page';

export default function DashboardTasksPage() {
  return <TasksPage />;
}
